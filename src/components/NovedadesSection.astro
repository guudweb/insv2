---
import type { Prestacion } from '../lib/strapi';
import { getStrapiImageUrl } from '../lib/strapi';

interface Props {
  prestaciones: Prestacion[];
}

const { prestaciones } = Astro.props;
---

<div class="portfolio-col">
  <div class="container">
    <h2 class="card-header">NOVEDADES</h2>
    <div class="row">
      {prestaciones.map((prestacion) => {
        const imageUrl = prestacion.imagen ? getStrapiImageUrl(prestacion.imagen.url) : '/images/default-prestacion.jpg';
        const slug = prestacion.slug;

        return (
          <div class="col-lg-3 col-md-4 col-sm-6 portfolio-item">
            <div class="card h-100">
              <img class="card-img-top" src={imageUrl} alt={prestacion.titulo} />
              <div class="card-body">
                <h4>
                  <a href={`/prestacion/${slug}`}>{prestacion.titulo}</a>
                </h4>
                <p>{prestacion.resumen}</p>
                <br />
                <br />
                <a href={`/prestacion/${slug}`} class="btn btn-primary">
                  Seguir leyendo &rarr;
                </a>
              </div>
            </div>
          </div>
        );
      })}
    </div>
    <hr />
  </div>
</div>
